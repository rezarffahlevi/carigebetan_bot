const db = require('./config/db');
const telegramBot = require('./telegramBot');


// Running from here
console.log('\nbot cari gebetan is running');
// console.log('bot scheduler is running with data : ', data);
// test();
// telegramBot.send('@rezarffahlevi', test);
// SCHEDULER
// data.rules.time.hour.forEach((dt,i) => {
//   let rule = new schedule.RecurrenceRule();
//   rule.dayOfWeek = data.rules.day;
//   rule.hour = dt;
//   rule.minute = data.rules.time.minute[i];
//   // schedulerDiscord(rule, data.rules.type[i]);
//   // if(['clock in', 'clock out'].some(sm => sm == data.rules.type[i]))
//   //   scheduler(rule, data.rules.type[i]);
// })
